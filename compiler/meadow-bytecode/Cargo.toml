[package]
name = "meadow-bytecode"
description = "The Meadow bytecode: a fixed-width register instruction set, and the program image the runtime loads. The contract between the compiler's back end and `meadow-rts`."
version.workspace = true
edition.workspace = true

[lib]
name = "meadow_bytecode"
path = "src/lib.rs"

[dependencies]
meadow-core.workspace = true
meadow-intern.workspace = true
