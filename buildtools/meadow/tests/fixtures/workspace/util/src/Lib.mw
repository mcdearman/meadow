-- `util` package. Only `@pub` names are visible to dependents;
-- a plain `@pub(pkg)` would reach this package's other modules and stop there.

@pub fun double x = x * 2

@pub fun scale factor xs = map (\x -> x * factor) xs

-- private helper: not exported, so `app` cannot see it
fun secret x = x + 1
