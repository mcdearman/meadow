target
.vscode
.idea
*.log
.repos
.repl_history
.claude
# insta scratch: regenerated on any snapshot mismatch
*.pending-snap

# Built VS Code extension: rebuilt by `editors/vscode/build.sh`, and attached to
# each release by `release.yml`. A checked-in binary would only go stale.
editors/vscode/node_modules/
editors/vscode/*.vsix
