-- Std: the standard library root module.
--
-- Each `mod` below is a child module. Nothing here is in scope for a child
-- automatically -- a module reaches another only through an explicit `use`.

@pub(pack) mod Test
@pub(pack) mod Bool
@pub(pack) mod Ref
@pub(pack) mod Ordering
@pub(pack) mod Function
@pub(pack) mod Tuple
@pub(pack) mod Int
@pub(pack) mod Maybe
@pub(pack) mod Char
@pub(pack) mod Result
@pub(pack) mod Either
@pub(pack) mod Bits
@pub(pack) mod Bytes
@pub(pack) mod Yield
@pub(pack) mod Collections
@pub(pack) mod Sort
@pub(pack) mod State
@pub(pack) mod Exn
@pub(pack) mod Stream
@pub(pack) mod Random
@pub(pack) mod Fs
@pub(pack) mod Process
@pub(pack) mod String
@pub(pack) mod Path
@pub(pack) mod Json
@pub(pack) mod Time
@pub(pack) mod Test
@pub(pack) mod prelude
